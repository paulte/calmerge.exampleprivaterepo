---
name: Build Calendar

permissions:
  contents: write

"on":
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

  schedule:
    - cron: "0 */6 * * *"

  workflow_dispatch:

jobs:
  build:
    uses: paulte/calmerge/.github/workflows/build-calendar.yml@main
